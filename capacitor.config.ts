import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.menukeyboardng',
  appName: 'menu-keyboard-ng',
  webDir: 'www',
};

export default config;
